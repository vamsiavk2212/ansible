- name: variabled from files
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course information
    ansible.builtin.debug:
      msg: "Hello I am learning {{ COURSE }}, duration is {{ DURATION }}, Trainer is {{ TRAINER }}"